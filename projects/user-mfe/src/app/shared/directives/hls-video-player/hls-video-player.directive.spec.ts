import { HlsVideoPlayerDirective } from './hls-video-player.directive';

describe('HlsVideoPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new HlsVideoPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
