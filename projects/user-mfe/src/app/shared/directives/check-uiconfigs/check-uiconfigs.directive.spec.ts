import { CheckUiconfigsDirective } from './check-uiconfigs.directive';

describe('CheckUiconfigsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUiconfigsDirective();
    expect(directive).toBeTruthy();
  });
});
