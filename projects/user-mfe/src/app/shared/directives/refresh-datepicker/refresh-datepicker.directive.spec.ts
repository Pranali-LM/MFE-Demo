import { RefreshDatepickerDirective } from './refresh-datepicker.directive';

describe('RefreshDatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new RefreshDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
