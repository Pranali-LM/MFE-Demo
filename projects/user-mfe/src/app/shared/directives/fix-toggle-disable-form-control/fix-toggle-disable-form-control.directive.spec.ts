import { FixToggleDisableFormControlDirective } from './fix-toggle-disable-form-control.directive';

describe('FixToggleDisableFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FixToggleDisableFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
