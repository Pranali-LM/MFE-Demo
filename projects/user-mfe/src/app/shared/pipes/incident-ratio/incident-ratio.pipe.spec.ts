import { IncidentRatioPipe } from './incident-ratio.pipe';

describe('IncidentRatioPipe', () => {
  it('create an instance', () => {
    const pipe = new IncidentRatioPipe();
    expect(pipe).toBeTruthy();
  });
});
